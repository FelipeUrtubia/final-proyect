<br>
<div class="row">
  <div class="col-md-4">
    <br>
    <%= link_to 'Continuar comparndo', root_path, class: 'btn btn-primary'%>
  </div>
  <div class="col-md-3 offset-md-3">
    <br>
    <%= link_to 'Vaciar carro', orders_destroy_all_path, class: 'btn btn-warning', method: :delete%>
  </div>
</div>
<br><br>
<% if @orders.present? %>
  <table class="table table-striped table-hover">
    <tr class="thead">
      <th>Nombre del producto</th>

      <th>Cantidad</th>

      <th>Remover</th>
    </tr>
      <% @orders.each do |order|%>
        <tr>
          <td>
            <%= order.product.name%>
          </td>

          <td>
            <%= order.quantity%>
          </td>

          <td>
            <%= link_to order_path(order), class: 'btn btn-danger', method: :delete do%>
              <i class="fas fa-trash"></i>
            <%end%>
          </td>
        </tr>
      <%end%>
  </table>

  <%= link_to 'Enviar cotización!', root_path, class: 'btn btn-success text-right'%>
  <br><br><br><br>
<% else %>
  <br><br><br>
  <h5>No Tienes nada en tu carrito...</h5>
  <br><br><br><br><br><br><br><br><br><br>
<% end %>
